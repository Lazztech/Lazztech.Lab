{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "serve docs",
            "type": "shell",
            "command": "cd docs/ && npm i && npm run serve"
        },
        {
            "label": "ssh MicroServer",
            "type": "shell",
            "command": "ssh gian@192.168.1.11"
        },
        {
            "label": "ssh UDM",
            "type": "shell",
            "command": "ssh root@192.168.1.1"
        }
    ]
}